import { SiteScrapper } from "../../domain";
import { BrowserActions, BrowserActionsParams } from "../../domain/interfaces";

class Scrapper implements SiteScrapper {
  getScenario(name: string) {}

  //   performScenario (name: string) {};

  //   performAction (
  //     action: BrowserActions,
  //     args: BrowserActionsParams
  //   ){}

  //   getStream(){}
}
